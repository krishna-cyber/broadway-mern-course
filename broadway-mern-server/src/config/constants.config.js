const fileFilterType = {
    IMAGE:'image',
    DOC:'doc',
    VIDEO:'video',
    AUDIO:'audio'
}

const userRoles = {
    ADMIN: 'admin',
    CUSTOMER: 'customer',
    SELLER: 'seller',
}

const statusType ={
    ACTIVE:'active',
    INACTIVE:'inactive'
}

module.exports = {
    fileFilterType,
    userRoles,
    statusType
}
